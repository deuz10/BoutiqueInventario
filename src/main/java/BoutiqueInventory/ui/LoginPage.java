package boutiqueInventory.ui;

import javax.swing.*;

public class LoginPage extends JFrame {
    public LoginPage() {
        setTitle("Login");
        setSize(300, 200);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        // Agregar componentes para la interacción del usuario
    }
}